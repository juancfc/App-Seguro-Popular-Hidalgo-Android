package com.repss.apprepss;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.view.WindowManager;

/**
 * Se muestra los requisitos para darse de alta en el seguro popular, todo esta asignado desde el xml de la vista
 */
public class RequisitosActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_requisitos);
    }
}
